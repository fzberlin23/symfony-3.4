{% extends 'base.html.twig' %}

{% block body %}
    <h1>Create new product</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}

    <br/>
    <a href="{{ path('products') }}"><< Back to products</a>
{% endblock %}
